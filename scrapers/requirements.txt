crawl4ai==0.9.2
supabase>=2.9.1
openai>=1.68.2
langchain-text-splitters==1.1.2
pyyaml==6.0.2
python-dotenv==1.2.2
pydantic>=2.10.0
playwright==1.52.0
aiohttp>=3.14.3
html2text>=2024.2.26
curl_cffi>=0.7.0
anthropic>=0.40.0
pytest>=8.0.0

# Transitive dependencies of crawl4ai/langchain, pinned to a floor here
# because crawl4ai's own declared ranges don't force these high enough on
# a fresh install and older resolutions carry known CVEs (see pip-audit).
pillow>=12.3.0
lxml>=6.1.0
cryptography>=50.0.0
pyopenssl>=26.4.0
h2>=4.4.1
langsmith>=0.8.18
nltk>=3.10.0
