import { GuestFormOnboarding } from "@/components/onboarding/GuestFormOnboarding";

/** Multi-step form onboarding for unauthenticated guests. */
export default function FormOnboardingPage() {
  return <GuestFormOnboarding />;
}
