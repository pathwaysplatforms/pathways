import { GuestTokenCleanup } from "@/components/onboarding/GuestTokenCleanup";

/** Plane view — content rendered by NavigationPlane in DashboardShell. */
export default function DashboardPage() {
  return <GuestTokenCleanup />;
}
